type: custom:apexcharts-card
graph_span: 6h
header:
  show: true
  title: AC/DC/Battery Temperatures
  show_states: true
  colorize_states: true
all_series_config:
  stroke_width: 3
  opacity: 0.3
  show:
    legend_value: false
color_list:
  - red
  - green
  - pink
series:
  - entity: sensor.sunsynk_temp_data
    name: AC Temp
    data_generator: |
      return entity.attributes.actime.map((actime, index) => {
        return [new Date(actime).getTime(), entity.attributes.acvalue[index]];
      });
  - entity: sensor.sunsynk_temp_data
    name: DC Temp
    data_generator: |
      return entity.attributes.dctime.map((dctime, index) => {
        return [new Date(dctime).getTime(), entity.attributes.dcvalue[index]];
      });
  - entity: sensor.sunsynk_battery_temp
    name: Battery Temp
